name: flybrain
channels:
  - conda-forge
dependencies:
  - python=3.13
  - numpy
  - scipy
  - pandas
  - pyarrow          # reads the MaleCNS feather files
  - pyyaml
  - matplotlib       # the figure
  - pytest
